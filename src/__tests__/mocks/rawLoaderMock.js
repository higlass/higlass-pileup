// Stub for webpack's raw-loader! imports (e.g. `import X from 'raw-loader!../dist/worker.js'`).
// Jest doesn't understand webpack loader syntax, so this returns an empty string.
module.exports = '';
